<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %><br>
  <%= @post.content %><br>
  </p>
  <% @post.tags.each do |tag| %>
    <p><%= tag.name %></p><br>
    <% end %>



<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
